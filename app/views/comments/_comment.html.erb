<p><%= comment.name %></p>
<%= comment.user.email %>

<date><%= time_ago_in_words(comment.created_at) %></date>

<p><%= comment.body %></p>

<% if user_signed_in? %>
  <p><%= link_to 'Delete', [comment.feature, comment], method: :delete, data: { confirm: 'Are you sure?' } %></p>
<% end %>
